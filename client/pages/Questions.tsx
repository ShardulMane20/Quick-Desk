import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Questions() {
  return (
    <PlaceholderPage
      title="Question Feed"
      description="Browse questions with titles, descriptions, answer counts, upvote/downvote buttons, and tag filters."
    />
  );
}
