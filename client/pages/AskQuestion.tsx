import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AskQuestion() {
  return (
    <PlaceholderPage
      title="Post a Question"
      description="Create a form with question title, description, and tags fields with a blue 'Post' button."
    />
  );
}
